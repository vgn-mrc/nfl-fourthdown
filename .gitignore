# Python
__pycache__/
*.py[cod]
*.ipynb_checkpoints

# Envs / tooling
.venv/
.uv/
.env

# Data (keep out of repo), but DO track model artifacts for deployment
data/raw/
data/processed/
artifacts/
models/
# Allow artifacts to be committed (needed for HF Space & Docker build)
!notebooks/artifacts_coach/
!notebooks/artifacts_wp/
!notebooks/artifacts_comp/
